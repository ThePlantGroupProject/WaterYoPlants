import re

f= open('data_in_text.txt', 'r', encoding = 'utf-8')
fout=open('filtered_data.txt', 'w', encoding='utf-8')

for line in f:
    line=f.read()
    til= re.search(re.compile(r' /d/d/d/), line)
    #print(line)
    print(til)
